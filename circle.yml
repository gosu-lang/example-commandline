machine:
  timezone:
    America/Los_Angeles
  java:
    version:
      oraclejdk8
  environment:
    TERM: dumb

general:
  branches:
    only:
      - master # only build/deploy from master

test:
  # Call './gradlew build uploadArchives' instead of the default 'mvn integration-test'
  override:
    - ./gradlew inlineScript simpleGosuProgram gosuProgramWithArgs gosuProgramWithImports gosuProgramWithClasspath
